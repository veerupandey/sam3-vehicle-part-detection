# Start from the inference/runtime dependencies.
-r requirements-local-mps.txt

# Dependencies imported by vendor/sam3/train/train.py and the trainer.
hydra-core>=1.3,<1.4
omegaconf>=2.3,<2.4
submitit>=1.5
fvcore>=0.1.5
scipy>=1.11
opencv-python-headless>=4.8
tqdm>=4.66
tensorboard>=2.14
